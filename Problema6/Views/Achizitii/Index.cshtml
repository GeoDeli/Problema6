@model IEnumerable<Problema6.Models.Achizitie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.Nume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Telefon.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataAchizitie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pret)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.Nume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefon.Model)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataAchizitie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pret)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_achizitie }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_achizitie }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_achizitie })
        </td>
    </tr>
}

</table>
